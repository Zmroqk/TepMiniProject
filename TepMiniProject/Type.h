#pragma once
enum class Type{
	None,
	INT,
	FLOAT,
	DOUBLE,
	STRING,
};


/*Type operator|(Type left, Type right);
Type operator&(Type left, Type right);
Type operator~(Type status);
void operator|=(Type& left, Type right);
void operator&=(Type& left, Type right);
bool operator==(Type left, Type right);
bool operator!=(Type left, Type right);*/